•	Separated all database/business logic using the MVC pattern by separating the pages, classes, and model
•	Routed all the URL’s in the index.php file using the fat free template
•	Connected to the database and used PDO in the index.php file and the database.php file
•	Data can be added in the estimate, style, and contact pages, and viewed in the summary pages
•	We both committed to the final project repo that was created by Vlad
•	Used OOP with the estimate and information files
•	Contains full Docblocks for all PHP files.
•	The form validates if the user incorrectly types in a value, or leaves it blank
•	Incorporated jquery in the home page where the picture display as a slide show

